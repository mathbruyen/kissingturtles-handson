/**
 * Function that draws corresponding data into the given canvas.
 *
 * Canvas cheatsheet:
 * - ctx.save();
 * - ctx.restore();
 * - ctx.moveTo(x, y);
 *
 * - ctx.lineWidth = 5;
 * - ctx.lineCap = 'round';
 * - ctx.strokeStyle = 'rgba(162, 141, 199, 0.8)';
 * - ctx.lineTo(x, y);
 * - ctx.beginPath();
 * - ctx.closePath();
 *
 * - ctx.fillStyle = "rgba(10,115,255,0.5)";
 * - ctx.fillRect(30, 30, 50, 50);
 *
 * Data format:
 * {
 * }
 *
 * @param ctx 2D context of the canvas element.
 * @param data data to draw.
 * @param size the size of the grid.
 **/
window.ktDraw = (function() {
  return function(ctx, data, size) {
    // TODO
  };
})();